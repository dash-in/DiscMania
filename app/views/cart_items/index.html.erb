<div class="container">
 <div class="row">
  <h2>カート</h2>
  <p class="border">※一回のご注文で複数の商品をご注文いただいた場合は、原則として全ての商品が揃ってから一括発送いたします。</p>
  <br>
  <table width="100%" height="100%">
  <tr align="center">
    <td></td>
  	<td>商品</td>
  	<td>単価</td>
  	<td>数量</td>
  	<td>金額</td>
  </tr>
  	  <% @cart_item.each do |record| %>
      <tr align="center">
  	  <td><%= attachment_image_tag @record,:image, format: 'jpeg', size: "100×100", fallback: "no_image.jpg", size:'100x100' %></td>
  	  <td>
        <%= record.name %><br>
        <%= record.artist.name %><br>
        <%= record.label %>
      </td>
      <td>￥<%= record.price.to_s(:delimited) %>（税込）</td>
      <td>1git</td>
      <td>￥<%= record.price.to_s(:delimited) %>（税込）</td>
      <td><button class="btn btn-danger">カートから削除</button></td>
      <% end %>
  </tr>
  </table>
  <div class="total">税込合計金額</div>
 </div>
</div>